import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    position: "relative",
    maxWidth: 345,
    maxHeight: "100vh",
    backgroundColor: "#555",
    color: "#fff",
  },
  media: {
    paddingTop: "56.25%",
    height: 0,
  },
}));
